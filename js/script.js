const gridElem = document.querySelector (".grid")
for (let i = 1; i <= 100; i++) {

    let oddEven;
    let color;

    if (i % 15 === 0 ) {
        console.log ("FizzBuzz")
        oddEven = "divisibileper15"
        color = ".fizzbuzz"
    }
}